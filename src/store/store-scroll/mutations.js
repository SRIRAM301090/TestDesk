export function setCurrentScrollPosition(state, scrollPosition) {
  state.currentScrollPosition = scrollPosition;
}

export function setScrollSections(state, sections) {
  state.scrollSections = sections;
}

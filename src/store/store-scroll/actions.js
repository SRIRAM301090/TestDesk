export function setScrollSections({ commit }, sections) {
  commit("setScrollSections", sections);
}

export function setCurrentScrollPosition({ commit }, scrollPosition) {
  commit("setCurrentScrollPosition", scrollPosition);
}
